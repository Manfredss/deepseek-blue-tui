# The environment variable takes precedence over the key saved by /login.
DEEPSEEK_API_KEY=sk-your-key

# Optional OpenAI-compatible endpoint override.
DEEPSEEK_BASE_URL=https://api.deepseek.com

# Optional isolated data directory (useful for development and tests).
# DEEPSEEK_TUI_HOME=/path/to/data
